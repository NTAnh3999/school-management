# Working defaults for local dev — no secrets, safe to commit. See .env.example for docs.
# Personal overrides go in .env.local (gitignored) instead of editing this file.
VITE_API_PROXY_TARGET=http://localhost:4000
VITE_PORT=3001
